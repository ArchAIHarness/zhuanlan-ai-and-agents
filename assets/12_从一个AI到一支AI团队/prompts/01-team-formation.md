---
output: ../01-team-formation.png
aspect_ratio: "16:9"
type: framework
style_reference: ../../09_RAG的关键从来不是向量/01-retrieval-is-the-core.png
---

Match the visual style of the reference image exactly: hand-drawn educational framework diagram with warm cream paper background, solid black sketch lines (not faint pencil), soft but clear pastel color blocks in orange/green/blue, handwritten Chinese labels, simple icons, no dark background, no 3D, no realistic people, subtle "ArchAIHarness" watermark bottom-right.

CRITICAL — color fill: every robot MUST be filled with soft pastel color (orange, green, or blue), like the reference image. Do NOT leave robots as plain white line-art outlines.

Topic: 空文件夹起步，先在脑子里把"主编是唯一出口"的班子队形摆好。

Composition:
- On the LEFT, a small empty folder icon (open, nothing inside) with a handwritten label "空工作间" — the starting point.
- A dotted arrow pointing RIGHT to a TEAM FORMATION diagram drawn like an org chart:
  - TOP: ONE lead robot (green, bigger), labeled "主编", with a small crown or "唯一出口" tag — this is the single point that talks to the user.
  - BELOW it, connected by lines, THREE worker robots in a row, each a soft pastel color with one tool and a label: "调研员" (magnifying glass), "撰稿" (pen), "校对" (red check pen).
  - A single line goes UP from 主编 to a small person/user icon at the very top, showing the lead is the only one facing the user.
- The message: you start from an empty folder, but first arrange the formation in your head — the lead is the only exit.

Robots: friendly rounded heads, one antenna, dot eyes, NO realistic faces.

Spelling exact and CRITICAL: labels must be exactly "空工作间" "主编" "唯一出口" "调研员" "撰稿" "校对". The writer label must be exactly "撰稿" — render "撰" as 提手旁 (扌) + 巽, do NOT write "搣稿" or other wrong variants.

Key insight: start from an empty folder, but first set the formation — one lead as the single exit, workers below.
