---
output: ../02-role-files.png
aspect_ratio: "16:9"
type: framework
style_reference: ../../09_RAG的关键从来不是向量/01-retrieval-is-the-core.png
---

Match the visual style of the reference image exactly: hand-drawn educational framework diagram with warm cream paper background, solid black sketch lines (not faint pencil), soft but clear pastel color blocks in orange/green/blue, handwritten Chinese labels, simple icons, no dark background, no 3D, no realistic people, subtle "ArchAIHarness" watermark bottom-right.

CRITICAL — color fill: every card MUST be filled with soft pastel color (orange, green, or blue), like the reference image. Do NOT leave things as plain white line-art outlines.

Topic: 四个角色文件各自的"身份证"——mode 定身份，permission 焊死边界。

Composition: FOUR small "ID card" style cards laid out in a 2x2 grid or a row, each card represents one role file (a Markdown file). Each card has:
- A small file icon (.md) at the top corner
- The role name in handwritten Chinese (big): "主编" / "调研员" / "撰稿" / "校对"
- Two short fields written like an ID card, exact labels: "mode：" (身份) and "permission：" (边界)
- A small lock icon next to permission to show the boundary is "焊死" (welded shut)

Use distinct pastel colors per card. Keep each card clean and readable.

Top title (handwritten Chinese): "四张角色身份证".

Robots optional: a tiny robot head icon on each card matching the role. NO realistic faces.

Spelling exact and CRITICAL: labels must be exactly "四张角色身份证" "主编" "调研员" "撰稿" "校对" "mode" "permission". The writer card must be exactly "撰稿" — render "撰" as 提手旁 (扌) + 巽, do NOT write "搣稿".

Key insight: each role is a file with an identity (mode) and a hard-welded boundary (permission).
