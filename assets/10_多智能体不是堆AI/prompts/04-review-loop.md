---
output: ../04-review-loop.png
aspect_ratio: "16:9"
type: framework
style_reference: ../../09_RAG的关键从来不是向量/01-retrieval-is-the-core.png
---

Match the visual style of the reference image exactly: hand-drawn educational framework diagram with warm cream paper background, solid black sketch lines (not faint pencil), soft but clear pastel color blocks in orange/green/blue, handwritten Chinese labels, simple icons, no dark background, no 3D, no realistic people, subtle "ArchAIHarness" watermark bottom-right.

CRITICAL — color fill: both robots MUST be filled with a soft pastel color (e.g. 撰稿 in orange, 校对 in blue), like the reference image. Do NOT leave robots as plain white line-art outlines; they must have warm colored body fills.

Topic: 评审兜底回路——撰稿出初稿、校对只挑错打回、撰稿重写、再过审。

Composition (a clear LOOP drawn with arrows in a cycle):
- Node A (left): friendly robot labeled "撰稿" holding a pen, producing a document card labeled "初稿".
- Arrow right to Node B (right): friendly robot labeled "校对" holding a red check pen, looking at the doc with a magnifier; it outputs a small card labeled "问题清单" with a couple of red marks. IMPORTANT label near 校对: "只挑错，不改稿".
- Arrow looping back from 校对 to 撰稿 labeled "打回重写".
- A separate green arrow leaving the loop labeled "过审 → 出稿" with a green checkmark, only after passing.
- The cycle visually shows draft → review → bounce back → rewrite → pass.

Spelling exact and CRITICAL: the writer role label must be exactly "撰稿" (zhuàn gǎo). Render the character "撰" correctly — 提手旁 (扌) on the left plus 巽 on the right. Do NOT write it as "搣稿" or any wrong variant. Other labels: "校对" "初稿" "问题清单" "只挑错，不改稿" "打回重写" "过审". Short labels only.

Key insight: the writer and the reviewer must be different roles; the reviewer only flags problems and bounces back, never edits — that loop is the quality safety net.
