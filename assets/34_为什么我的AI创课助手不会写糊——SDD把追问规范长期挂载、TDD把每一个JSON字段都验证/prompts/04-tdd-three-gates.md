手绘教育插图风格, 暖色调奶油纸背景(#F5F0E8), 黑色草图线条(#2C2C2C), 柔和粉彩色块(橙色#E8A87C 绿色#85BAA1 蓝色#7BA7C9 棕色#D4A574 红色#E07A5F 黄色#F2CC8F 紫色#B5A8C7)

画面构图: 画面纵向展示三道自动闸门(从左到右排列, 每道闸门都是"铁门+红绿灯"风格), 每道闸门旁标"校验规则清单"; 三道闸门右下方画两条分叉(✓ 通过 / ✗ 退回重做); 三道闸门下方标"AI 时代 TDD = 试菜员常驻厨房门口"; 强调三道闸共同保证"AI 写不出糊的 JSON"

三道自动闸门(从左到右, 纵向排列, 占画面 65%):

第一道闸「追问覆盖度闸」(最左, 占画面 22%):
- 简笔铁门(灰色 #B5B5B5 填充, 黑色细边, 圆角矩形)
- 门上挂一个红绿灯(简笔, 红灯亮 + 绿灯暗, 暗示"如果不通过")
- 门上方手写体醒目标签(粗黑): '追问覆盖度闸'
- 门上标(手写体小字, 红色): '每轮覆盖 ADD 8 维度 ≥ 1'
- 门下方标(手写体小字, 红色): '4 轮累计不能漏 5 个以上'
- 门右侧列出校验规则清单(米色 #F2E8D5 卡片, 圆角, 黑色细边):
  * 卡片顶部小标签(粗黑): '校验规则清单'
  * 卡片内手写体小字(纵向逐行):
    '□ 学员分析已覆盖
    □ 目标分析已覆盖
    □ 任务分析已覆盖
    □ 结构/内容已覆盖
    □ 练习/反馈已覆盖
    □ 难度曲线已覆盖
    第 5 段必须汇总确认'

第二道闸「JSON 合法性闸」(中部, 占画面 22%):
- 简笔铁门(灰色 #B5B5B5 填充, 黑色细边, 圆角矩形)
- 门上挂一个红绿灯(红灯亮)
- 门上方手写体醒目标签(粗黑): 'JSON 合法性闸'
- 门上标(手写体小字, 红色): '必填 / enum / 数值 / 引用完整性'
- 门下方标(手写体小字, 红色): 'AI 写错直接拦回'
- 门右侧列出校验规则清单(蓝色 #B5D5E8 卡片, 圆角, 黑色细边):
  * 卡片顶部小标签(粗黑): '校验规则清单'
  * 卡片内手写体小字(纵向逐行):
    '□ courseName / description 必填
    □ scenario ∈ {enterprise, k12, ...}
    □ contentType ∈ {text, exam, lab}
    □ estimatedHours > 0
    □ score > 0
    □ prerequisites 引用完整
    用 jsonschema 库校验'

第三道闸「修改一致性闸」(最右, 占画面 22%):
- 简笔铁门(灰色 #B5B5B5 填充, 黑色细边, 圆角矩形)
- 门上挂一个红绿灯(红灯亮)
- 门上方手写体醒目标签(粗黑): '修改一致性闸'
- 门上标(手写体小字, 红色): '改字段 → 关联字段必须重算'
- 门下方标(手写体小字, 红色): '改完不重算 → 退回重做'
- 门右侧列出校验规则清单(绿色 #C5DBC9 卡片, 圆角, 黑色细边):
  * 卡片顶部小标签(粗黑): '校验规则清单'
  * 卡片内手写体小字(纵向逐行):
    '□ totalHours 改后 estimatedHours 重算
    □ 删除章节时 prerequisites 必清除
    □ 改 difficulty 时艾宾浩斯曲线重算
    □ 改 bloomLevel 时 examConfig 重算
    跑 diff 脚本自动校验'

三道闸门下方标注(画面下方, 横向, 醒目):
- 一条粗黑长线, 标签牌(白底黑字, 醒目手写体): 'AI 时代 TDD = 试菜员常驻厨房门口'
- 标签牌下方小字(手写体, 暖橙色): '每一份 JSON 出锅前必走这三道闸'

三道闸门右下方分叉(画面右下方, 占画面 15%):
- 两条分叉箭头(从三道闸门底部向右下方分叉):
  * 上方一条绿色箭头(粗黑边, 绿色填充), 箭头标 ✓(粗黑), 终点标签: '通过 · 入库'
  * 下方一条红色虚线(回退路径), 箭头标 ✗(粗黑), 终点回到画面左侧的 AI 小机器人(简笔橙色 AI 圆头), 标签: '退回 · 重做'
- 分叉上方标一行字(手写体, 醒目): '通过 ✓ / 退回 ✗'

底部一行手写体粗标(画面最下方居中, 醒目, 黑色): 'AI 不懂 ADD · 但 AI 能把 ADD 跑得比人更稳'
底部小字(手写体, 红色): 'TDD 是焊死的闸 · 不是装饰 · CI/CD 流水线自动拦'

背景点缀几个小图标(简笔线稿, 散落画面边缘): 铁门、红绿灯、勾选 ✓、叉 ✗、AI 小机器人、虚线、校验规则清单卡片

右下角微妙 ArchAIHarness 水印(微小不抢眼球)

16:9 横版, 无深色背景无3D无写实人物
