# 34 篇配图索引

> **文章**:为什么我的 AI 创课助手不会写糊——SDD 把追问规范长期挂载、TDD 把每一个 JSON 字段都验证
> **风格**:手绘教育插图风格(hand-drawn educational framework diagram) — 暖色调奶油纸背景 / 黑色草图线条 / 柔和粉彩色块(主色橙绿蓝) / 手写体中文标签 / 右下角 ArchAIHarness 水印
> **生成工具**:豆包 Seedream 5.0 Lite(baoyu-image-gen batch mode)
> **生成日期**:2026-07-02

---

## 封面 cover

- **文件**:`cover.png`
- **brief 图编号**:图 1(封面)
- **核心概念**:专栏 34 篇首图——AI 创课助手 = 一间按 DDD/SDD/TDD 三件套标准搭好的智能厨房。横向全景展示三工种分区(course 切配 / question 装盘 / media 仓储)+ 厨房门口站一位戴高帽的试菜员(TDD 持 ✓/✗ 双面章)+ 厨房四墙挂满各种菜谱表(SDD Spec 端长期挂载),顶部横幅"DDD 三工种 + SDD 墙菜谱表 + TDD 试菜员 = 智能厨房秩序"
- **视觉锚点**:三工种分区(米色切配台 / 蓝色装盘台 / 绿色仓储货架)+ 试菜员 + 墙上 5 张菜谱表(ADD 8 维度 / JSON Schema 9 字段 / 题型规则 / 难度规则 / 4 轮+1 汇总硬约束)+ 顶部金句 + 底部"AI 创课助手 = 一间按秩序搭好的厨房"
- **不插入正文**:✅ 仅作各平台发布封面

---

## 正文配图(按出现顺序)

### 01 — 业务模型到工程模型:33 JSON 拆解为 34 三限界上下文

- **文件**:`01-business-to-engineering-model.png`
- **brief 图编号**:图 2(01)
- **对应正文位置**:`@@@IMG_1@@@`(正文第 27 行,引言段"下面一段一段拆"之后,§一"业务模型有了..."之前)
- **核心概念**:把 33 篇产出的"标准化业务模型 JSON"拆解成 34 篇的三限界上下文(course / question / media)+ 上下文映射(共享内核+共享规范);展示从"业务"到"工程"的转换路径——业务边界即工程边界
- **视觉锚点**:左侧完整 JSON 文档(顶层 9 字段:courseName / description / totalHours / scenario / targetAudience / bloomTaxonomy / structure / knowledgePoints / examConfig)+ 中间粗黑箭头标"DDD 战略设计 / 业务边界 = 工程边界"+ 右侧三限界上下文分叉树(course 米色 / question 蓝色 / media 绿色)+ 三条双向虚线(共享内核 / 共享规范 / 事件订阅)
- **正文引用**:`![业务模型到工程模型的转换路径——33 篇产出的标准化业务模型 JSON,通过 DDD 战略设计拆解为三限界上下文(course 切配 / question 装盘 / media 仓储)+ 三条上下文映射关系,业务边界即工程边界](assets/34_为什么我的AI创课助手不会写糊——SDD把追问规范长期挂载、TDD把每一个JSON字段都验证/01-business-to-engineering-model.png)`

---

### 02 — DDD 三限界上下文:工种分区 + 上下文映射 + 共享内核

- **文件**:`02-ddd-three-bounded-contexts.png`
- **brief 图编号**:图 3(02)
- **对应正文位置**:`@@@IMG_2@@@`(正文第 187 行,§三"DDD —— 三限界上下文切分"末尾"下面这张图把三限界上下文 + 上下文映射 + 共享内核画出来"之后)
- **核心概念**:详解三限界上下文的"工种分区"——每个上下文有自己的通用语言、聚合根、上下文映射关系;不混领域语言,各管一段
- **视觉锚点**:横向三个工种分区(course 切配 / question 装盘 / media 仓储)+ 每个分区挂一张"通用语言清单"卡片(列出本上下文关键词:course/chapter/section/kp/prerequisite/bloomLevel/estimatedHours 等)+ 三个工种区之间用双向虚线连接(标"Customer-Supplier · Feign 同步" / "Published Language · Kafka 事件" / "Shared Kernel · 共享内核")+ 底部共享内核长线"布鲁姆动词表 + 知识点 ID 规范 + prerequisites 字段"+ 对角线斜批"DDD = 工种分区 · 边界焊死 · 选择放开"
- **正文引用**:`![DDD 三限界上下文 + 上下文映射关系——course 切配工种 / question 装盘工种 / media 仓储工种三个工种分区,各自有通用语言清单(聚合根),三条双向虚线连接(Customer-Supplier / Published Language / Shared Kernel),底部共享内核(布鲁姆动词表 + 知识点 ID 规范 + prerequisites 字段)三仓共同遵守,对角线斜批"DDD = 工种分区 · 边界焊死 · 选择放开"](assets/34_为什么我的AI创课助手不会写糊——SDD把追问规范长期挂载、TDD把每一个JSON字段都验证/02-ddd-three-bounded-contexts.png)`

---

### 03 — SDD 双端:墙上挂菜谱表 + 服务员点菜单

- **文件**:`03-sdd-two-ends.png`
- **brief 图编号**:图 4(03)
- **对应正文位置**:`@@@IMG_3@@@`(正文第 236 行,§四"SDD 双端"末尾"下面这张图把 SDD 双端的双轨结构画出来"之后)
- **核心概念**:SDD 双端架构——墙上挂菜谱表(Spec 端,长期挂载,副厨/AI 进厨房必读)+ 服务员点菜单(Story 端,工程师与老师业务对话脚本)。Spec 端是"规矩",Story 端是"活儿";Spec 焊边界,Story 跑业务
- **视觉锚点**:画面分左右两栏,中间粗虚线"SDD 双端"+ 左栏大白墙上挂满 5 张菜谱表(ADD 8 维度追问菜谱表 / JSON Schema 9 字段校验菜谱表 / 题型规则菜谱表 / 难度规则菜谱表 / 4 轮+1 汇总硬约束菜谱表——最右红色相框标"焊死")+ 墙下方副厨正在读菜谱表+ 右栏桌面场景(老师戴学士帽 + 蓝色围裙服务员端空白菜单本 + 桌中央半杯咖啡 + 老师气泡"我要做 Java 培训,40 小时,企业培训场景" / 服务员气泡"我按墙上菜谱表帮你把菜式列出来")
- **正文引用**:`![SDD 双端架构——左栏墙上挂满各种菜谱表(Spec 端,长期挂载,含 ADD 8 维度 / JSON Schema 9 字段 / 题型规则 / 难度规则 / 4 轮+1 汇总硬约束),右栏服务员(工程师)接收老师点菜单(Story 端),中间粗虚线连接"SDD 双端",Spec 端是"墙上规矩",Story 端是"服务员对话",Spec 焊边界 / Story 跑业务](assets/34_为什么我的AI创课助手不会写糊——SDD把追问规范长期挂载、TDD把每一个JSON字段都验证/03-sdd-two-ends.png)`

---

### 04 — TDD 三层护栏:追问覆盖度闸 + JSON 合法性闸 + 修改一致性闸

- **文件**:`04-tdd-three-gates.png`
- **brief 图编号**:图 5(04)
- **对应正文位置**:`@@@IMG_4@@@`(正文第 291 行,§五"TDD 三层护栏"末尾"下面这张图把三道闸画出来"之后)
- **核心概念**:TDD 在 AI 时代是三道焊死的闸——追问覆盖度闸(每轮必须覆盖 ADD 8 维度)、JSON 合法性闸(必填字段 / enum 范围 / 数值范围 / 引用完整性)、修改一致性闸(用户改 JSON 后关联字段必须重新校验);三道闸共同保证"AI 写不出糊的 JSON"
- **视觉锚点**:画面横向展示三道自动闸门(每道闸门都是铁门+红绿灯样式)+ 每道闸门右侧挂"校验规则清单"卡片(米色 / 蓝色 / 绿色)+ 闸门之上标"AI 时代 TDD = 试菜员常驻厨房门口 / 每一份 JSON 出锅前必走这三道闸"+ 右下方分叉两条路径(绿色 ✓ 通过入库 / 红色 ✗ 退回重做——退回路径回到 AI 小机器人)+ 底部"AI 不懂 ADD · 但 AI 能把 ADD 跑得比人更稳" / "TDD 是焊死的闸 · 不是装饰 · CI/CD 流水线自动拦"
- **正文引用**:`![TDD 三层测试护栏——三道自动闸门(追问覆盖度闸 / JSON 合法性闸 / 修改一致性闸),每道闸门配红绿灯 + 校验规则清单卡片(必填字段 / enum 范围 / 数值范围 / 引用完整性 / 关联字段重算),右下方分叉两条路径(✓ 通过入库 / ✗ 退回重做),底部"AI 时代 TDD = 试菜员常驻厨房门口","AI 不懂 ADD · 但 AI 能把 ADD 跑得比人更稳"](assets/34_为什么我的AI创课助手不会写糊——SDD把追问规范长期挂载、TDD把每一个JSON字段都验证/04-tdd-three-gates.png)`

---

### 05 — 价值复利:一次创建多次复用(资产沉淀池)

- **文件**:`05-value-compound.png`
- **brief 图编号**:图 6(05)
- **对应正文位置**:`@@@IMG_5@@@`(正文第 364 行,§七"资产沉淀"末尾"下面这张图把'一次创建、多次复用'画出来"之后)
- **核心概念**:AI 创课助手产出的资产可被多次复用——一次创建"ADD 8 维度菜谱表" + "JSON Schema 9 字段菜谱表" + "题型规则菜谱表" + "难度规则菜谱表" + "4 轮+1 汇总硬约束菜谱表" + "追问模板库(三场景)" + "布鲁姆动词表 36 词" + "真实课程实例 JSON",后续任何课程(企业培训 / K12 / 大学 / 数据分析)都可套用
- **视觉锚点**:画面中央大圆形"资产沉淀池"(深橙色边框,内填浅黄色),池里装满 8 张可复用资产卡片(每张卡片正面写名称)+ 池左侧 AI 创课助手(白厨帽 + 橙色围裙)正在注入,气泡"AI 创课助手首次创建"+ 池右侧三条彩色分叉箭头(新业务调用 → Java 培训 / 新场景复用 → Python 培训 / 新团队复用 → 数据分析培训)+ 对角线斜批"一次创建 · 多次复用 = 价值复利"+ 底部"脱离时间换钱 · 个人业务资产持续沉淀" / "5 类资产 + AI 创课助手骨架 = 一次创建 · 多次复用 · 价值复利"
- **正文引用**:`![价值复利——一次创建多次复用,中央资产沉淀池装满 8 张可复用资产卡片(ADD 8 维度菜谱表 / JSON Schema 9 字段菜谱表 / 题型规则 / 难度规则 / 4 轮+1 汇总硬约束 / 追问模板库企业培训·K12·大学 / 布鲁姆动词表 36 词 / 真实课程实例 Java 40h JSON),左侧 AI 创课助手首次创建注入池,右侧三条分叉箭头(新业务调用 / 新场景复用 / 新团队复用)分别指向 Java 培训 / Python 培训 / 数据分析培训,对角线斜批"一次创建 · 多次复用 = 价值复利"](assets/34_为什么我的AI创课助手不会写糊——SDD把追问规范长期挂载、TDD把每一个JSON字段都验证/05-value-compound.png)`

---

## 目录结构

```
assets/34_为什么我的AI创课助手不会写糊——SDD把追问规范长期挂载、TDD把每一个JSON字段都验证/
├── README.md                                              # 本索引文件
├── batch.json                                             # 批量生成配置(baoyu-image-gen)
├── cover.png                                              # 封面(不插入正文)
├── 01-business-to-engineering-model.png                   # 正文图 1
├── 02-ddd-three-bounded-contexts.png                      # 正文图 2
├── 03-sdd-two-ends.png                                    # 正文图 3
├── 04-tdd-three-gates.png                                 # 正文图 4
├── 05-value-compound.png                                  # 正文图 5
└── prompts/                                               # prompt 源文件
    ├── cover.md
    ├── 01-business-to-engineering-model.md
    ├── 02-ddd-three-bounded-contexts.md
    ├── 03-sdd-two-ends.md
    ├── 04-tdd-three-gates.md
    └── 05-value-compound.md
```

---

## 质量自检(2026-07-02)

- [x] 6 张图全部生成(5 正文 + 1 封面)
- [x] 中文标签清晰可读(主要标题/概念词 100% 可读,部分细节辅助文字允许稍小)
- [x] 风格统一(暖色奶油纸 #F5F0E8 + 黑线 #2C2C2C + 粉彩主色橙#E8A87C/绿#85BAA1/蓝#7BA7C9,符合专栏 §5.6 二)
- [x] 与 27-33 期配图风格连贯(同款厨师帽+围裙工种分区视觉语言)
- [x] 每张图与 brief 的"核心概念"对齐
  - 01:业务模型 → 工程模型 转换路径
  - 02:DDD 三限界上下文 + 上下文映射 + 共享内核
  - 03:SDD 双端(墙上规矩 + 服务员对话)
  - 04:TDD 三层闸门(覆盖度 + 合法性 + 修改一致性)
  - 05:价值复利(一次创建,多次复用)
- [x] 5 处 `@@@IMG_N@@@` 全部替换为 `![alt](path)` 形式
- [x] 封面 16:9 比例正确
- [x] 无深色背景、无 3D、无写实人物(符合 §5.6 二禁忌)
- [x] 右下角 ArchAIHarness 水印均在
- [x] README 索引完整

---

## 主编决断

按规则,本期配图已全部完成,提交主编校对。发布动作由主编手动触发,发布后链接由主编反馈给插画专员归档(对齐 §7.0 / §7.3)。
