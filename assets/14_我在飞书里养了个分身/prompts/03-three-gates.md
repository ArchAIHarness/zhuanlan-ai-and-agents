---
output: ../03-three-gates.png
aspect_ratio: "16:9"
type: framework
style_reference: ../../09_RAG的关键从来不是向量/01-retrieval-is-the-core.png
---

Match the visual style of the reference image exactly: hand-drawn educational illustration with warm cream paper background, solid black sketch lines (not faint pencil), soft but clear pastel color blocks in orange/green/blue, handwritten Chinese labels, simple icons, no dark background, no 3D, no realistic people, subtle "ArchAIHarness" watermark bottom-right.

CRITICAL — color fill: every block/gate MUST be filled with soft pastel color (orange, green, or blue), like the reference image. Do NOT leave things as plain white line-art outlines.

Topic: 三道闸，焊死"它说完就一定真发出去"—— 事前用规矩挑明、事中盯着它做没做、事后没做就自动替它补发。

Composition: a left-to-right flow with THREE GATES (draw three turnstile/gate shapes in sequence) that a message must pass through to truly reach the group:
- Gate 1 (orange), label "事前 · 立规矩": a rule note pinned up reading "最后必须真发出去". caption "把丑话说在前头".
- Gate 2 (green), label "事中 · 盯着做没做": an eye / checklist watching whether the "发消息" hand was actually used, with a checkbox. caption "记一笔：发了没？".
- Gate 3 (blue), label "事后 · 没做就替它补": a system hand auto-pushing the unsent message out to the group. caption "漏了，系统自动补发".
On the far left a robot says a line in a thought bubble ("好的，我回复了…"); show that WITHOUT the gates it might NOT actually send (a small dotted "没发出去" leak); WITH the three gates, the message truly lands in the group on the far right (a solid chat bubble landing in a group, label "真的发到群里").

Bottom strip caption (handwritten Chinese): "人定规矩 · AI 执行 · 系统兜底审计".

Top title (handwritten Chinese): "三道闸：说了就一定发出去".

Spelling exact and CRITICAL: labels must be exactly "三道闸：说了就一定发出去" "事前 · 立规矩" "最后必须真发出去" "事中 · 盯着做没做" "记一笔：发了没？" "事后 · 没做就替它补" "漏了，系统自动补发" "真的发到群里" "人定规矩 · AI 执行 · 系统兜底审计". Use only short labels.

Robot: friendly rounded head, one antenna, dot eyes, NO realistic face.

Key insight: don't rely on the AI's good behavior — three gates (rule upfront, watch during, auto-fix after) guarantee the message is actually sent.
