---
output: ../01-clone-into-feishu.png
aspect_ratio: "16:9"
type: framework
style_reference: ../../09_RAG的关键从来不是向量/01-retrieval-is-the-core.png
---

Match the visual style of the reference image exactly: hand-drawn educational illustration with warm cream paper background, solid black sketch lines (not faint pencil), soft but clear pastel color blocks in orange/green/blue, handwritten Chinese labels, simple icons, no dark background, no 3D, no realistic people, subtle "ArchAIHarness" watermark bottom-right.

CRITICAL — color fill: every object and robot MUST be filled with soft pastel color (orange, green, or blue), like the reference image. Do NOT leave things as plain white line-art outlines.

Topic: 同一个 AI 分身，被搬进飞书后，给每个聊天单开一条独立的记忆线，互不串味。

Composition: on the LEFT, a single friendly robot inside a plain isolated chat box (label "一个 AI 分身"). A big arrow points RIGHT into a chat app, where the SAME robot now connects to THREE separate chat threads stacked vertically, each thread drawn as its own little notepad / memory line with its own dots of memory:
- thread 1 label "私聊" (1-on-1)
- thread 2 label "A 群"
- thread 3 label "B 群"
Draw the three memory lines clearly SEPARATE (a small wall/divider between them, or three distinct colored notepads), to show they do NOT mix. A tiny caption under them: "各记各的，互不串味".

Top title (handwritten Chinese): "一聊一会话".

Spelling exact and CRITICAL: labels must be exactly "一个 AI 分身" "私聊" "A 群" "B 群" "各记各的，互不串味" "一聊一会话". Use only short labels.

Robot: friendly rounded head, one antenna, dot eyes, NO realistic face.

Key insight: the same AI clone keeps a separate memory thread for every chat, so it has continuity in each without leaking across them.
