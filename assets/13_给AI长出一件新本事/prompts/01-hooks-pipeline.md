---
output: ../01-hooks-pipeline.png
aspect_ratio: "16:9"
type: framework
style_reference: ../../09_RAG的关键从来不是向量/01-retrieval-is-the-core.png
---

Match the visual style of the reference image exactly: hand-drawn educational framework diagram with warm cream paper background, solid black sketch lines (not faint pencil), soft but clear pastel color blocks in orange/green/blue, handwritten Chinese labels, simple icons, no dark background, no 3D, no realistic people, subtle "ArchAIHarness" watermark bottom-right.

CRITICAL — color fill: every block MUST be filled with soft pastel color (orange, green, or blue), like the reference image. Do NOT leave things as plain white line-art outlines.

Topic: 把 AI 干活想象成一条流水线，每个节点留一个挂钩，插件就是往挂钩上挂自己的逻辑。

Composition: a horizontal PIPELINE / conveyor belt running left to right, with several work stations/nodes along it (each a small pastel box: 收到任务 → 动手前 → 干活 → 出结果). At each node there is a small HOOK (挂钩) shape drawn hanging off the belt. 
- On ONE of the hooks (e.g. the "动手前" node), a small plugin block (orange, lego-style with "+") is being HUNG onto the hook by a hand, with a label "插件" — showing you attach your own logic at that hook.
- Other hooks are empty, drawn as little open hook shapes, to show there are multiple hook points.

Top title (handwritten Chinese): "流水线上的挂钩".

Spelling exact and CRITICAL: labels must be exactly "流水线上的挂钩" "收到任务" "动手前" "干活" "出结果" "挂钩" "插件". Use only short labels.

Key insight: AI's work is a pipeline; each node leaves a hook, and a plugin is the logic you hang onto a hook.
