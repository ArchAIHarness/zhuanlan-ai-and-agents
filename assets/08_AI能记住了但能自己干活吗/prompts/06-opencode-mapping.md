# 06 OpenCode 怎么落地这些设计

## Style
Hand-drawn educational framework diagram on warm cream paper (#F5F0E8). Black sketch lines. Soft pastel blocks in orange, green, blue. Handwritten Chinese labels, minimal text. Simple line icons. Subtle "ArchAIHarness" watermark in bottom-right corner. No dark background, no 3D, no realistic people.

## Content
A left-to-right mapping table. The top title text must read EXACTLY "OpenCode 怎么落地这些设计" — spell the English word as "OpenCode" (O-p-e-n-C-o-d-e), do not misspell it.

Two columns with a hand-drawn arrow (→) between each row. Left column header "设计概念", right column header "OpenCode 实现". Five rows, each left cell is a small rounded sketch box (concept) and each right cell is a rounded sketch box (implementation) connected by a sketch arrow:

**Row 1**: Left "指令层" (blue). → Right "AGENTS.md：写一行规则，行为就变".
**Row 2**: Left "工具系统" (orange). → Right "注册制：注册清单 = 能力边界".
**Row 3**: Left "权限分层" (green). → Right "Plan / Build 模式：只看 vs 能改".
**Row 4**: Left "上下文管理" (blue). → Right "自动整理：满了就压缩成摘要".
**Row 5**: Left "错误处理" (orange). → Right "失败 3 次就停 + 问人". The number 3 means a retry cap of three times; write the Chinese clearly as "失败 3 次就停 + 问人", do not use any wrong character.

Each row neatly aligned. All arrows are hand-drawn sketch style pointing right. Handwritten Chinese labels, clear and readable. Simple line art throughout.
