---
output: ../05-one-core-many-faces.png
aspect_ratio: "16:9"
type: framework
style_reference: ../../09_RAG的关键从来不是向量/01-retrieval-is-the-core.png
---

Match the visual style of the reference image exactly: hand-drawn educational framework diagram with warm cream paper background, solid black sketch lines (not faint pencil), soft but clear pastel color blocks in orange/green/blue, handwritten Chinese labels, simple icons, no dark background, no 3D, no realistic people, subtle "ArchAIHarness" watermark bottom-right.

CRITICAL — color fill: every block MUST be filled with a soft pastel color (orange, green, or blue), like the reference image. Do NOT leave things as plain white line-art outlines.

Topic: 一个内核 + 社区零件 + 多形态对外 —— "积木 + 变形"图。

Composition: center-and-radiate layout.
- CENTER: one solid rounded core box (green) labeled "一个内核", the heartpiece.
- AROUND the core (left/top), a few lego-style add-on blocks (orange) snapping onto it, each a small "+" piece, with a small label "社区零件" — community-made parts you can plug in.
- FROM the core, arrows radiate OUT to FOUR different "faces" / outlets on the right, each a distinct small icon + handwritten Chinese label:
  - "终端" (a command-line window icon)
  - "网页" (a browser window icon)
  - "编辑器" (a code editor icon)
  - "服务端" (a server/cloud icon)
- The message: same one core, plug in community parts, and it can take many forms for different uses.

Spelling exact and CRITICAL: labels must be exactly "一个内核" "社区零件" "终端" "网页" "编辑器" "服务端". Use only short labels.

Key insight: one open core, plug in community-made parts, and reshape it into many forms (terminal / web / editor / server) — extensible and productizable.
