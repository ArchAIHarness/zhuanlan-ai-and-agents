---
output: ../01-retrieval-is-the-core.png
aspect_ratio: "16:9"
type: framework
---

Create a 16:9 hand-drawn educational framework diagram in the same visual style as existing ArchAIHarness illustrations:
- warm cream paper background
- black sketch lines
- soft pastel blocks in orange, green, blue
- handwritten Chinese labels, minimal text
- simple icons
- no dark background, no 3D, no realistic people
- subtle "ArchAIHarness" watermark bottom-right

Topic: RAG 的内核是"检索到对的内容"，不是向量库——别把手段当目的。

Composition:
- Top label: "RAG ＝ 检索增强生成"
- Show three connected boxes in a left-to-right flow with arrows:
  - Box 1 "检索" (retrieval) — highlighted in bright orange, with a magnifying glass icon, marked with a small star and a label "命门在这" (the crux is here)
  - Box 2 "增强" (augment) — pale blue, a small document-feeding-into icon
  - Box 3 "生成" (generate) — pale green, a small chat/answer bubble icon
- Below the flow, a crossed-out icon of a database cylinder labeled "向量库" with a small caption "只是一种手段，不是 RAG 本身"
- Make the "检索" box visually dominant to show it is the key.

Key insight to visualize: Among the three steps of RAG, retrieval is the decisive one; the vector database is just one optional means, not RAG itself.

Use only short labels, avoid long explanations.
