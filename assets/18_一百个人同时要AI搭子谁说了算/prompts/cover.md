---
output: ../cover.png
aspect_ratio: "16:9"
type: cover
style_reference: ../../09_RAG的关键从来不是向量/01-retrieval-is-the-core.png
---

Match the visual style exactly: hand-drawn educational illustration, warm cream paper, solid black sketch lines, soft pastel color fill, handwritten Chinese labels, simple icons, no dark background, no 3D, no realistic people, subtle "ArchAIHarness" watermark.

CRITICAL — color fill every element.

This is a COVER image (16:9), used as article cover.

Main title (large handwritten Chinese, top): "一百个人同时要AI搭子，谁说了算？"
Subtitle (smaller below): "我写了个调度大脑，但真正的设计是三条死规矩"

Visual element: CENTER a large BRAIN-shaped control panel (orange fill) with multiple wires/connections fanning DOWN to many small robot icons (each labeled "用户 N" number on the body) sitting in individual workstations. The brain has THREE large switches/buttons clearly drawn on it: (1) "归属焊死 —人一实例" (2) "创建可写重启不碰" (3) "TTL 到期自动回收". Below each switch a small check mark. Background shows two giant blocks labeled "K8s" and "Redis" as the shoulders. A tiny timer icon at the bottom is expiring — signal TTL auto-reclaim.

Spelling exact: "一百个人同时要AI搭子，谁说了算？" "我写了个调度大脑，但真正的设计是三条死规矩" "归属焊死 —人一实例" "创建可写重启不碰" "TTL 到期自动回收" "K8s" "Redis".

Key insight: a scheduling brain that manages hundreds of AI buddies by three inviolable design rules — identity, state, and lifecycle.
