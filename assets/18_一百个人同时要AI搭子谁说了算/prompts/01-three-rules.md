---
output: ../01-three-rules.png
aspect_ratio: "16:9"
type: framework
style_reference: ../../09_RAG的关键从来不是向量/01-retrieval-is-the-core.png
---

Match the visual style exactly: hand-drawn educational illustration, warm cream paper, solid black sketch lines, soft pastel color fill, handwritten Chinese labels, simple icons, no dark background, no 3D, no realistic people, subtle "ArchAIHarness" watermark.

CRITICAL — color fill every element.

Topic: 调度大脑的三条设计规矩——归属、状态、资源。

Composition: THREE large vertical columns, each with a rule number on top, a bold label, and a visual.
- COLUMN 1 (orange): "规则①" "一人一个实例，归属焊死" — a person icon with a shield labeled "x-user-id", an X over "客户端自报", a check over "上游鉴权注入". Caption: "谁叫你，谁就是你".
- COLUMN 2 (green): "规则②" "创建可写重启不碰" — a timeline showing "→ 首次创建 O_EXCL (写文件)" → "→ 重启只 mkdir -p (绝不写)". Big red STOP sign on "重启写文件". Caption: "状态可预测".
- COLUMN 3 (blue): "规则③" "TTL 当警察，不信任常驻" — a clock/timer counting down, with a heartbeat arrow "每5分钟续约" going up before the timer hits zero, and a "到期自动回收" arrow going down if timer expires. Caption: "不是等人走，是到期收".

Top title: "调度不是代码，是三条谁也不能越界的规矩".

Spelling exact: "调度不是代码，是三条谁也不能越界的规矩" "一人一个实例，归属焊死" "x-user-id" "客户端自报" "上游鉴权注入" "谁叫你，谁就是你" "创建可写重启不碰" "O_EXCL (写文件)" "重启只 mkdir -p (绝不写)" "重启写文件" "状态可预测" "TTL 当警察，不信任常驻" "每5分钟续约" "到期自动回收" "不是等人走，是到期收".

Key insight: scheduling is not about code, it's about three inviolable design rules.
